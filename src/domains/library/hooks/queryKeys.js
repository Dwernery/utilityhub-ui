// Centralized React Query key constants for the library domain so call
// sites (and cache invalidations) can't drift out of sync with typos.
export const BOOKS_KEY = ["books"];
export const AUTHORS_KEY = ["authors"];
export const SERIES_KEY = ["series"];
